# Contracts

This directory is the single source of truth for shared domain types and API contracts. Every layer of the project should import or mirror these definitions so the frontend, orchestrator, and MCP servers stay aligned.
