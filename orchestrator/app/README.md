# Orchestrator App

This directory will contain the FastAPI application code for the DM orchestrator. It will host the HTTP/SSE surface, route incoming client requests, and coordinate the agent loop with the LLM client and MCP tool calls.
