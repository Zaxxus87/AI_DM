# Orchestrator LLM Adapter

This directory will house the LLM adapter layer and abstractions for `LLMClient`, including Gemini and optional Claude implementations. It will translate orchestrator intent into model calls and normalize model outputs into tool instructions.
