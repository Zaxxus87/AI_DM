[project]
name = "orchestrator"
version = "0.1.0"
description = "Agentic DM orchestrator: LLMClient adapter (Gemini) + MCP client + CLI agent loop"
requires-python = ">=3.11"
dependencies = [
    "google-genai>=1.0.0",
    "mcp>=1.2.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
