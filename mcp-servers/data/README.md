# MCP Servers — Data

This directory will contain MCP tool servers for game state, encounter tracking, and session memory. These services manage Firestore state, event logs, and deterministic updates to game entities.
