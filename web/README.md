# Web

This directory will host the Vite + React + TypeScript frontend for the Agentic Dungeon Master. It will implement the chat UI, party sheet, encounter tracker, map browser, and DM configuration panels.
