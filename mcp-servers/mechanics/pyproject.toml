[project]
name = "dice-mechanics"
version = "0.1.0"
description = "Stateless dice-mechanics MCP server (D&D 2024) over Streamable HTTP"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.2.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
