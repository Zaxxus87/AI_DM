# Ingestion

This directory will contain the ingestion pipeline for the rules corpus, lore modules, and map metadata. It will prepare data for embeddings and RAG search without implementing runtime game logic.
