# MCP Servers — Rules

This directory will contain MCP tools for rules retrieval, RAG search, and reference lookup. It will expose the 2024 D&D rulebook corpus as deterministic read-only tools for the orchestrator.
